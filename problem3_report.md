# Problem #3: Achieving Best Performance

## 결과

지금까지 제출 결과 중 가장 좋은 성능은 **Exp 3 (mAP@0.5 = 0.28740)** 이고, baseline인 Exp 2 (0.27233) 대비 약 0.015 정도 향상됐다. Exp 4는 학습 중이며, 결과가 나오면 더 좋은 쪽을 최종 제출로 사용할 예정이다.

| 실험 | 핵심 변경 | mAP@0.5 |
| --- | --- | --- |
| Exp 2 (baseline) | 기준 설정 | 0.27233 |
| **Exp 3 (현재 best)** | 해상도/loss/LR/conf threshold 종합 조정 | **0.28740** |
| Exp 4 | Exp 3에서 해상도만 더 키움 (batch/epoch 축소) | 학습 중 |

## 하이퍼파라미터 변경 표

세 실험 모두 모델 구조(DETR + ResNet-50)와 TODO 구현 코드는 동일하며, 하이퍼파라미터만 다르다.

| 항목 | Exp 2 | Exp 3 | Exp 4 |
| --- | --- | --- | --- |
| 입력 해상도 (shortest / longest) | 480 / 800 | **600 / 1000** | **720 / 1200** |
| BBox / GIoU matching cost | 5 / 2 | **7 / 3** | 7 / 3 |
| BBox / GIoU loss coefficient | 5 / 2 | **7 / 3** | 7 / 3 |
| EOS coefficient | 0.1 | **0.02** | 0.02 |
| backbone / transformer LR | 1e-5 / 1e-4 | **5e-6 / 5e-5** | 5e-6 / 5e-5 |
| inference confidence threshold | 0.5 | **0.3** | 0.3 |
| BATCH_SIZE | 4 | 4 | **1** |
| NUM_EPOCHS | 50 | 50 | **20** |

(굵은 글씨 = 직전 실험 대비 변경된 항목)

## Exp 2 → Exp 3에서 바꾼 것 (왜)

- **이미지 해상도를 키운 이유**: VOC2007에는 작은 물체(병, 새 같은 것)가 많은데, 이미지가 작으면 이런 작은 물체들이 흐릿해져서 모델이 잘 못 찾는다. 그래서 이미지를 더 크게(480→600) 키워서 작은 물체도 잘 보이게 했다.
- **박스 위치 관련 가중치(BBox/GIoU)를 키운 이유**: Exp 2 결과를 보니, 모델이 "이게 무슨 물체인지" 분류는 잘하는데 "물체가 정확히 어디 있는지" 위치 맞추는 건 약했다. 그래서 박스 위치 정확도에 더 신경 쓰도록 관련 가중치를 키웠다.
- **EOS coefficient(no-object 가중치)를 낮춘 이유**: DETR은 한 이미지마다 항상 100개의 후보를 예측하는데, VOC 이미지에는 물체가 보통 3개 정도밖에 없다. 그래서 100개 중 97개는 "물체 없음"으로 학습되고, 모델이 점점 "아무것도 없다"고 답하는 쪽으로 치우친다. EOS 값을 0.1→0.02로 낮춰서 모델이 좀 더 적극적으로 물체를 찾게 했다.
- **learning rate를 절반으로 낮춘 이유**: DETR 원래 설정은 COCO(이미지 약 12만 장) 기준인데, VOC(약 5천 장)는 그보다 25배 작다. 작은 데이터에 같은 학습률을 쓰니 처음에 loss가 들쭉날쭉했다. 절반으로 낮추니 안정적으로 학습됐다.
- **예측 신뢰도(confidence threshold)를 낮춘 이유**: DETR은 100개 후보에 점수가 나눠져서, 정답 후보의 점수도 0.5보다 낮게 나오는 경우가 많다. 그래서 0.5로 자르면 정답까지 버려졌다. 0.3으로 낮추니 정답이 더 많이 살아남아서 mAP가 올라갔다.

## Exp 3 → Exp 4에서 바꾼 것 (왜)

Exp 3에서 한꺼번에 6가지를 바꿨기 때문에, 그 중 어느 게 실제로 효과가 있었는지는 알 수 없다. 가장 효과가 컸을 것 같은 **이미지 해상도** 만 따로 더 키워보면 그 효과를 확인할 수 있다. 그래서 Exp 4에서는 해상도만 720/1200으로 더 올리고, 나머지(loss, LR, conf threshold)는 Exp 3 그대로 뒀다. 다만 이미지가 커지면 GPU 메모리가 더 필요해서 한 번에 처리하는 이미지 수(BATCH_SIZE)를 1로 줄였고, 시간이 부족해서 학습 epoch도 50→20으로 줄였다.

- Exp 4가 Exp 3보다 점수가 높으면 → "해상도를 키우는 게 제일 효과적이었다"는 게 맞다.
- Exp 4가 비슷하거나 더 낮으면 → 학습 epoch을 너무 줄여서 충분히 학습 못 한 탓일 수도 있고, 해상도 효과 자체가 작을 수도 있다. 둘 중 어느 쪽인지 알려면 epoch을 다시 늘려서 돌려봐야 한다.

## 종합 한계

한 번에 여러 하이퍼파라미터를 같이 바꿨기 때문에 개별 기여도를 분리한 ablation은 진행하지 못했다. 가장 영향이 클 것으로 짐작하는 건 **(1) 해상도** 와 **(2) confidence threshold** 이고, loss/LR 조정은 학습을 안정시키는 보조 역할로 본다. Exp 4 결과를 통해 (1)의 단독 효과를 일부 검증할 예정이며, 시간이 허락하는 대로 깨끗한 ablation 실험을 진행하는 것이 다음 단계다.
