# Problem #3: Achieving Best Performance

## 결과

가장 좋은 성능은 **Exp 3 (mAP@0.5 = 0.33968)** 이고, baseline인 Exp 1 (0.27233) 대비 약 **+0.067 (상대 향상 약 24.6%)** 향상됐다.

| 실험 | 핵심 변경 | mAP@0.5 |
| --- | --- | --- |
| Exp 1 (baseline) | 기준 설정 | 0.27233 |
| Exp 2 | 해상도 ↑ + loss 가중치 강화 + EOS ↓ + conf threshold ↓ | 0.28740 |
| **Exp 3 (best)** | Exp 2에서 해상도만 더 키움 (batch/epoch 축소) | **0.33968** |

## 하이퍼파라미터 변경 표

세 실험 모두 모델 구조(DETR + ResNet-50)와 TODO 구현 코드는 동일하며, 하이퍼파라미터만 다르다. 옵티마이저는 모든 실험에서 동일하게 `Adam(lr=1e-5)` 단일 그룹을 사용했다 (LR scheduler 없음, weight decay 없음).

| 항목 | Exp 1 | Exp 2 | Exp 3 |
| --- | --- | --- | --- |
| 입력 해상도 (shortest / longest) | 480 / 800 | **600 / 1000** | **800 / 1333** |
| BBox / GIoU matching cost | 5 / 2 | **7 / 3** | 7 / 3 |
| BBox / GIoU loss coefficient | 5 / 2 | **7 / 3** | 7 / 3 |
| EOS coefficient | 0.1 | **0.02** | 0.02 |
| inference confidence threshold | 0.5 | **0.3** | 0.3 |
| BATCH_SIZE | 4 | 4 | **2** |
| NUM_EPOCHS | 50 | 50 | **25** |

(굵은 글씨 = 직전 실험 대비 변경된 항목)

## Exp 1 → Exp 2에서 바꾼 것 (왜)

- **이미지 해상도를 키운 이유**: VOC2007에는 작은 물체(병, 새 같은 것)가 많은데, 이미지가 작으면 이런 작은 물체들이 흐릿해져서 모델이 잘 못 찾는다. 그래서 이미지를 더 크게(480→600) 키워서 작은 물체도 잘 보이게 했다.
- **박스 위치 관련 가중치(BBox/GIoU)를 키운 이유**: Exp 1 결과를 보니, 모델이 "이게 무슨 물체인지" 분류는 잘하는데 "물체가 정확히 어디 있는지" 위치 맞추는 건 약했다. 그래서 박스 위치 정확도에 더 신경 쓰도록 매칭 cost와 loss coefficient를 모두 5/2 → 7/3으로 키웠다.
- **EOS coefficient(no-object 가중치)를 낮춘 이유**: DETR은 한 이미지마다 항상 100개의 후보를 예측하는데, VOC 이미지에는 물체가 보통 3개 정도밖에 없다. 그래서 100개 중 97개는 "물체 없음"으로 학습되고, 모델이 점점 "아무것도 없다"고 답하는 쪽으로 치우친다. EOS 값을 0.1 → 0.02로 낮춰서 모델이 좀 더 적극적으로 물체를 찾게 했다.
- **예측 신뢰도(confidence threshold)를 낮춘 이유**: DETR은 100개 후보에 점수가 나눠져서, 정답 후보의 점수도 0.5보다 낮게 나오는 경우가 많다. 그래서 0.5로 자르면 정답까지 버려졌다. 0.3으로 낮추니 정답이 더 많이 살아남아서 mAP가 올라갔다.

결과적으로 mAP가 0.27233 → 0.28740으로 향상됐지만, 한 번에 4가지를 같이 바꿨기 때문에 어느 변경이 얼마나 기여했는지는 이 단계에서는 분리할 수 없었다.

## Exp 2 → Exp 3에서 바꾼 것 (왜)

Exp 2의 4가지 변경 중 가장 효과가 컸을 것으로 짐작한 것은 **이미지 해상도**였다. 이를 검증하기 위해 Exp 3에서는 **해상도만 800/1333으로 더 키우고, 나머지(loss 가중치, EOS, conf threshold)는 Exp 2 설정 그대로 유지**했다. 다만 해상도 증가로 GPU 메모리 부담이 커져서 BATCH_SIZE를 4 → 2로 줄였고, 학습 시간 제약으로 NUM_EPOCHS도 50 → 25로 절반으로 축소했다.

**결과**: Exp 3가 0.33968로, **학습 epoch이 절반밖에 안 됐음에도 불구하고 Exp 2 대비 +0.052 (+18% 상대 향상)** 라는 큰 폭의 점수 상승을 보였다. 이는 두 가지를 시사한다:

1. **해상도가 가장 영향이 큰 knob이었다**는 가설이 강하게 지지된다. Exp 2에서 한꺼번에 바꾼 4가지 중 해상도가 점수 향상의 주요 동력이었을 가능성이 높다.
2. **학습량이 줄었음에도 점수가 올랐다**는 사실은, 작은 객체(병, 새, 화분 등) 검출이 저해상도에서 구조적으로 누락되고 있었고 고해상도가 이를 회복시켰다는 해석을 뒷받침한다. 단순한 학습 시간 부족 문제가 아니라 입력 정보량의 문제였다는 의미다.

## 종합 한계 및 다음 단계

- **Ablation 부재**: Exp 2에서 4가지 하이퍼파라미터를 동시에 변경했기 때문에 개별 기여도를 정확히 분리하지는 못했다. Exp 3 (해상도만 단독으로 추가 변경)의 큰 점수 상승은 해상도가 가장 영향이 큰 knob임을 정성적으로 시사하지만, loss 가중치/EOS/conf threshold 각각의 효과를 따로 측정하려면 추가 실험이 필요하다.
- **분리 LR / scheduler 미적용**: Exp 2/Exp 3의 cell 32에 `BACKBONE_LR=5e-6`, `TRANSFORMER_LR=5e-5`, `WEIGHT_DECAY=1e-4` 변수를 정의했으나, 옵티마이저 정의 라인(`optim.Adam(model.parameters(), lr=1e-5)`)에 연결하지 못해 실제로는 모든 실험이 단일 LR Adam을 사용했다. AdamW + 분리 LR + StepLR/CosineAnnealingLR 적용은 추가 향상 여지가 남아 있는 부분이다.
- **데이터 증강 미사용**: RandomHorizontalFlip, ColorJitter 같은 표준 증강을 적용하지 않았다. 일반적으로 큰 향상이 기대되는 영역이며 시간이 허락하면 우선 시도할 다음 단계다.
- **Exp 3 학습량 부족**: Exp 3가 25 epoch에서 멈췄음에도 best였다는 점은, **같은 해상도(800/1333)로 50 epoch을 채워 학습하면 추가 향상이 가능**할 것임을 강하게 시사한다.

**최종 제출**: Exp 3 (mAP@0.5 = 0.33968)
